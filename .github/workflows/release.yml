name: Release

on:
  push:
    branches:
      - release
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: "${{ github.ref }}"
  cancel-in-progress: true

jobs:
  determine-tag:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.determine-tag.outputs.tag }}
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@master
        with:
          test-bot: false

      - name: Determine tag
        id: determine-tag
        uses: Homebrew/actions/brew-script@master
        with:
          script: |
            require "formula"

            tag = Formula["portable-ruby"].pkg_version.to_s

            existing_tag = Utils.safe_popen_read("git", "tag", "--list", tag)
            raise "Tag #{tag} already exists" if existing_tag.present?

            puts "Preparing for #{tag} release."
            puts "::set-output name=tag::#{tag}"
  
  tag-test:
    needs: determine-tag
    runs-on: ubuntu-latest
    env:
      TAG: ${{ needs.determine-tag.outputs.tag }}
    steps:
      - name: Install gems
        run: echo "${TAG}"

  build:
    needs: determine-tag
    uses: ./.github/workflows/build.yml

  release:
    needs: [determine-tag, build]
    runs-on: ubuntu-latest
    env:
      TAG: ${{ needs.determine-tag.outputs.tag }}
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@master
        with:
          test-bot: false

      - name: Install gems
        run: brew install-bundler-gems

      - name: Configure Git user
        uses: Homebrew/actions/git-user-config@master

      - name: Setup directory
        run: mkdir bottles

      - name: Download bottles
        uses: actions/download-artifact@v3
        with:
          name: bottles
          path: bottles

      - name: Upload to GitHub Packages
        working-directory: bottles
        env:
          HOMEBREW_GITHUB_PACKAGES_USER: ${{ github.actor }}
          HOMEBREW_GITHUB_PACKAGES_TOKEN: ${{ github.token }}
        run: brew pr-upload --root-url=https://ghcr.io/v2/${GITHUB_REPOSITORY_OWNER,,}/portable-ruby

      - name: Push commits
        uses: Homebrew/actions/git-try-push@master
        with:
          token: ${{ github.token }}
        env:
          GIT_COMMITTER_NAME: BrewTestBot
          GIT_COMMITTER_EMAIL: 1589480+BrewTestBot@users.noreply.github.com

      - name: Push tag
        run: |
          gh release create "${TAG}" bottles/*.bottle.[0-9]*.tar.gz --notes "Portable Ruby ${TAG}"

      - name: Uplaod to GitHub Releases
        working-directory: bottles
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ github.token }}
        run: brew pr-upload --upload-only --root-url=https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}

      - name: Cleanup
        run: rm -rvf bottles
